#!/bin/bash
# if ! $(iw dev wlan0 info | grep -q ssid);then
#    exit
# fi
#if ! $(netstatus > /dev/null); then
  #exit
#fi

if ! (pidof hydroxide> /dev/null);then
    exit
fi

unread-count -u mizzunet -p $HYDROXIDE_BRIDGE_PASS -S 127.0.0.1 -P 1143