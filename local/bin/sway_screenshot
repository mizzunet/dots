#!/bin/bash
ACTION="$@"
    
case $ACTION in
    save)
        grim -g "$(slurp)" - | swappy -f -
        ;;
    copy)
        grim /tmp/screen.png && wl-copy < /tmp/screen.png && notify-send "Screenshot copied to clipboard"
        ;;
esac
